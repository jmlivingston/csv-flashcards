Quick and dirty flashcard card generator for printing from a CSV file. This can be run using the following steps.

Do download project dependencies run the following:
```
npm install
bower install
```

To start the project run the following:

```
grunt serve
```